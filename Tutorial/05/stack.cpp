#include <iostream>
#include <stack>
using namespace std;

int main(){
    // Modifying Stack (Add/Remove) Last-In First-Out เป็นเหมือนกล่องแนวตรงที่ข้อมูลตัวแรกจะอยู่ล่างสุด
    // Stack Function empty(),size(),top(),push(T element),pop()
    // Type top() return ตัวบนสุด
    // void push(T element) ใส่ข้อมูล
    // void pop() เอาข้อมูลออก
    // !!!Warning!!! 
    // top(),pop() ถ้า Stack ไม่มีข้อมูลจะ Error!!!
    // Stack ไม่มี Iterator*****
    // Example Usage of Stack : เช็ค Parenthesis(วงเล็บ),การทำ function call (เช่น การทำงานแบบ Stack ของ Java,JVM เป็น Stack Machine),การทำ undo/redo
    

    return 0;
}